
import { dateTimeFormater } from 'civ/helpers/date-time-formater';
import { module, test } from 'qunit';

module('Unit | Helper | date time formater');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateTimeFormater([42]);
  assert.ok(result);
});

